<table>
  <thead>
    <tr>
      <th>Text</th>
      <th>Translation</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @cards.each do |card| %>
      <tr>
        <td><%= card.text %></td>
        <td><%= card.translation %></td>
        <td><%= link_to 'Show', panel_card_path(card.id, panel_id: params[:id]) %></td>
        <td><%= link_to 'Edit', edit_panel_card_path(card.id, panel_id: params[:id]) %></td>
        <td><%= link_to 'Destroy', panel_card_path(card.id, panel_id: params[:id]), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
