<%= simple_form_for(@card) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :text %>
    <%= f.input :translation %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
