<%= simple_form_for(@card, url: panel_cards_path) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :text %>
    <%= f.input :translation %>
    <%= f.association :panel %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
